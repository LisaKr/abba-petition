<div class="container">
    <img src="/abba.png" />

    <h1>Sign my petition to make ABBA perform at every Eurovision until the end of time!</h1>

    {{#if error}}
        <div class="error">Oops! Something went wrong! Please try again. <br />Note that all fields must be filled out.</div>
        <audio autoplay>
            <source src="womp-womp.mp3" type="audio/mpeg">
        </audio>
        <br />
    {{/if}}

    <form method="POST">
        Signature: <br/>
        <input type="hidden" name="sig">
        <canvas id="signature" width="300" height="50"></canvas><br/>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <button type="submit">Submit</button>
    </form>

    <br />
    {{>logOut}}

</div>
